project Matcher is

   for Source_Dirs use ("src", "../common");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("match_times.adb");

   package Compiler is
      for Switches ("ada") use ("-gnatw.ufkpru", "-gnatybcdefux");
   end Compiler;

end Matcher;

