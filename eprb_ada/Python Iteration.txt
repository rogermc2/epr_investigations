FOR EACH index k AND EACH PAIR (i, j) IN setting_pairs:
    
    // Find matching positions in the data arrays
    Create boolean mask As where 'adeg' equals i
    Create boolean mask Bs where 'bdeg' equals j
    Create combined mask Ts where BOTH As and Bs are true
    
    // Extract filtered measurements for Alice and Bob
    Filter 'alice' array using mask Ts at column index 1, store in Ai
    Filter 'bob' array using mask Ts at column index 1, store in Bj
    
    // Calculate simulated and theoretical correlation values
    Multiply elements of Ai and Bj, then calculate the average, store in Cab_sim
    Convert angle difference (j - i) to radians
    Calculate quantum value using QMFunc with radians and 0.5, store in Cab_qm
    
END FOR

 For (A = [[10, 20], [30, 40], [50, 60]]). 
During the loop, the following occurs:
Iteration 0: k = 0, the row is [10, 20], i = 10, j = 20
Iteration 1: k = 1, the row is [30, 40], i = 30, j = 40
Iteration 2: k = 2, the row is [50, 60], i = 50, j = 60