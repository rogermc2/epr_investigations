with "config/eprb_ada_config.gpr";
project Eprb_Ada is

   for Source_Dirs use ("src/", "config/");
   for Object_Dir use "obj/" & Eprb_Ada_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use ".";
   for Main use ("eprb_ada.adb");

   package Compiler is
      for Default_Switches ("Ada") use Eprb_Ada_Config.Ada_Compiler_Switches;
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Eprb_Ada;
